import type { Project } from "@/types/content";

/**
 * Intentionally empty. Per project policy, no project photography, client
 * names, locations, or metrics are fabricated. Add real, verified entries
 * here (or wire this file up to a CMS/database call) once project imagery
 * and details are supplied — the Portfolio section and /projects route
 * already render this data and need no further code changes to go live.
 */
export const projects: Project[] = [];
